# strava-activity-map

A web app that loads all activities from a user's Strava account and displays them in a Leaflet app.

Pull git updates: ```git pull origin main```

Deploy new version to Google Cloud: ```gcloud app deploy```
